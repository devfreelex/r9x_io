import template from './appTitle.template'
import styles from './appTitle.styles'

const appTitle = () => {

    const tagName = 'app-title'

    return {
        tagName,
        template,
        styles  
    }

}

export { appTitle }